/**
 * tailwind.config.js
 *
 * ⚠️  SHIM ONLY — do not add config here.
 *
 * Next.js resolves tailwind.config.ts FIRST when both files exist.
 * All design tokens, colors, animations, and extensions are in tailwind.config.ts.
 *
 * This file exists only for:
 *   - Legacy tooling (PostCSS plugins, Storybook) that reads .js
 *   - CI tools that check for the .js extension
 */

// eslint-disable-next-line @typescript-eslint/no-require-imports
module.exports = require('./tailwind.config.ts');